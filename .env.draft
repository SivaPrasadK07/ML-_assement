# .env.draft — copy to .env and fill in real values before deploying

# Application settings
APP_HOST=0.0.0.0
APP_PORT=8000
APP_ENV=development      # development | production

# Model path (default: cluster_model.pkl in working directory)
MODEL_PATH=cluster_model.pkl

# Data path (used for fallback training when PKL is absent)
DATA_PATH=research_centers.csv
